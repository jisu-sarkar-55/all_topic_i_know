#include <iostream>
using namespace std;

int main() {

    // Print "Hello, World!" to the console
    cout << "Hello, World!" << endl;

    // Wait for user input before closing
    cin >> ws; // input will store in ws variable 
    
    return 0;
}